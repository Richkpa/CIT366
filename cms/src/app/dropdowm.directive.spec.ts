import { DropdowmDirective } from './dropdowm.directive';

describe('DropdowmDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdowmDirective();
    expect(directive).toBeTruthy();
  });
});
